<UserControl x:Class="RunescapePriceChecker.Wpf.Views.MoneyMakersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="32,26,32,30">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>

        <Grid Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="ACTIVITY CALCULATORS" Style="{StaticResource Kicker}" />
                <TextBlock Text="Money makers" Style="{StaticResource PageTitle}" Margin="0,3,0,0" />
                <TextBlock Text="Every method runs through the same item-in, item-out GP/hour engine."
                           Foreground="{StaticResource MutedBrush}" FontSize="14" Margin="0,5,0,0" />
            </StackPanel>
            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal">
                <Border Background="#E9DFC9" CornerRadius="5" Padding="14,9" Margin="0,0,10,0">
                    <StackPanel><TextBlock Text="PRICING RULE" Style="{StaticResource Kicker}" FontSize="9" /><TextBlock Text="High / low midpoint" FontWeight="SemiBold" Margin="0,2,0,0" /></StackPanel>
                </Border>
                <Button Content="Refresh prices" Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}" AutomationProperties.Name="Refresh calculator prices" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="300" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#252B26" CornerRadius="9" Margin="0,0,10,0" Padding="18">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                    <TextBlock Text="AVAILABLE METHODS" Style="{StaticResource Kicker}" Foreground="#D9AE61" Margin="8,4,0,14" />
                    <ListBox Grid.Row="1" ItemsSource="{Binding Methods}" SelectedItem="{Binding SelectedMethod}"
                             Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Margin" Value="0,0,0,8" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem"><ContentPresenter /></ControlTemplate></Setter.Value></Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border x:Name="MethodCard" Background="#1D211E" BorderBrush="#3B443C" BorderThickness="1" CornerRadius="6" Padding="12">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="36" /><ColumnDefinition /><ColumnDefinition Width="18" /></Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Index}" Foreground="#D2A65B" FontFamily="Georgia" FontWeight="Bold" VerticalAlignment="Center" />
                                        <StackPanel Grid.Column="1"><TextBlock Text="{Binding Name}" Foreground="White" FontWeight="SemiBold" /><TextBlock Text="{Binding Actions}" Foreground="#9FA79F" FontSize="10" Margin="0,3,0,0" /></StackPanel>
                                        <TextBlock Grid.Column="2" Text="›" Foreground="#D2A65B" FontSize="20" VerticalAlignment="Center" />
                                    </Grid>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True"><Setter TargetName="MethodCard" Property="Background" Value="#3A4435" /><Setter TargetName="MethodCard" Property="BorderBrush" Value="#977136" /></DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <Border Grid.Row="2" BorderBrush="#3B443C" BorderThickness="0,1,0,0" Padding="7,14,7,0">
                        <StackPanel><TextBlock Text="Modular by design" Foreground="White" FontWeight="SemiBold" FontSize="12" /><TextBlock Text="Add one method class; discovery and navigation are automatic." Foreground="#9FA79F" FontSize="10" Margin="0,5,0,0" LineHeight="15" /></StackPanel>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource Panel}" Margin="10,0,0,0" Padding="0">
                <ScrollViewer Padding="24" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <Border Margin="0,0,0,14">
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource Notice}"><Setter Property="Visibility" Value="Visible" /><Style.Triggers><DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger><DataTrigger Binding="{Binding ErrorMessage}" Value=""><Setter Property="Visibility" Value="Collapsed" /></DataTrigger></Style.Triggers></Style>
                            </Border.Style>
                            <TextBlock Text="{Binding ErrorMessage}" Foreground="#805415" />
                        </Border>

                        <Grid Margin="0,0,0,18">
                            <StackPanel>
                                <TextBlock Text="{Binding MethodKicker}" Style="{StaticResource Kicker}" />
                                <TextBlock Text="{Binding MethodName}" Style="{StaticResource SectionTitle}" Margin="0,4,0,0" />
                                <TextBlock Text="{Binding MethodDescription}" Foreground="{StaticResource MutedBrush}" Margin="0,6,150,0" />
                            </StackPanel>
                            <Border HorizontalAlignment="Right" VerticalAlignment="Top" Background="#E1F0E5" CornerRadius="12" Padding="12,5">
                                <TextBlock Text="LIVE CALCULATION" Foreground="{StaticResource PositiveBrush}" FontSize="9" FontWeight="Bold" />
                            </Border>
                        </Grid>

                        <Border Background="#282E29" CornerRadius="8" Padding="22" Margin="0,0,0,18">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="1.1*" /><ColumnDefinition Width="1.5*" /></Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="ESTIMATED PROFIT / HOUR" Foreground="#ADB4AD" FontSize="10" FontWeight="Bold" />
                                    <TextBlock Text="{Binding ProfitAllAccounts}" FontFamily="Georgia" FontSize="29" FontWeight="Bold" Margin="0,6,0,2">
                                        <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Foreground" Value="#E0897C" /><Style.Triggers><DataTrigger Binding="{Binding IsProfitPositive}" Value="True"><Setter Property="Foreground" Value="#77CEA0" /></DataTrigger></Style.Triggers></Style></TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="{Binding AccountSummary}" Foreground="#ADB4AD" FontSize="11" />
                                </StackPanel>
                                <Grid Grid.Column="1" Margin="20,0,0,0">
                                    <Grid.RowDefinitions><RowDefinition /><RowDefinition /></Grid.RowDefinitions><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition /></Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,12,10"><TextBlock Text="GROSS SALES" Foreground="#919991" FontSize="9" /><TextBlock Text="{Binding GrossSales}" Foreground="White" FontWeight="SemiBold" /></StackPanel>
                                    <StackPanel Grid.Column="1" Margin="12,0,0,10"><TextBlock Text="GE TAX" Foreground="#919991" FontSize="9" /><TextBlock Text="{Binding Tax}" Foreground="White" FontWeight="SemiBold" /></StackPanel>
                                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,10,12,0"><TextBlock Text="SUPPLIES" Foreground="#919991" FontSize="9" /><TextBlock Text="{Binding Supplies}" Foreground="White" FontWeight="SemiBold" /></StackPanel>
                                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="12,10,0,0"><TextBlock Text="PER ACCOUNT" Foreground="#919991" FontSize="9" /><TextBlock Text="{Binding ProfitPerAccount}" Foreground="#E8C57E" FontWeight="SemiBold" /></StackPanel>
                                </Grid>
                            </Grid>
                        </Border>

                        <Border Margin="0,0,0,14">
                            <Border.Style><Style TargetType="Border" BasedOn="{StaticResource Notice}"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding HasMissingPrices}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Border.Style>
                            <TextBlock Text="One or more items have no current trade price and contribute 0 gp to this estimate." Foreground="#805415" />
                        </Border>

                        <Grid Margin="0,0,0,8">
                            <StackPanel><TextBlock Text="CALCULATION LEDGER" Style="{StaticResource Kicker}" /><TextBlock Text="Hourly item flows" FontFamily="Georgia" FontSize="19" FontWeight="SemiBold" Margin="0,3,0,0" /></StackPanel>
                            <TextBlock Text="2% GE tax applies to outputs" Foreground="{StaticResource MutedBrush}" FontSize="10" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                        </Grid>

                        <Grid Background="#EDE5D5" Height="34">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="2.2*" /><ColumnDefinition Width="0.8*" /><ColumnDefinition Width="0.8*" /><ColumnDefinition Width="1.1*" /><ColumnDefinition Width="1.2*" /></Grid.ColumnDefinitions>
                            <TextBlock Text="ITEM" Margin="12,0" VerticalAlignment="Center" FontSize="9" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" />
                            <TextBlock Grid.Column="1" Text="FLOW" VerticalAlignment="Center" FontSize="9" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" />
                            <TextBlock Grid.Column="2" Text="QTY / HR" VerticalAlignment="Center" FontSize="9" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" />
                            <TextBlock Grid.Column="3" Text="UNIT PRICE" VerticalAlignment="Center" FontSize="9" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" />
                            <TextBlock Grid.Column="4" Text="HOURLY VALUE" VerticalAlignment="Center" FontSize="9" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" />
                        </Grid>
                        <ItemsControl ItemsSource="{Binding FlowRows}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid MinHeight="48" Background="#FCFAF4">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="2.2*" /><ColumnDefinition Width="0.8*" /><ColumnDefinition Width="0.8*" /><ColumnDefinition Width="1.1*" /><ColumnDefinition Width="1.2*" /></Grid.ColumnDefinitions>
                                        <Border Grid.ColumnSpan="5" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1" />
                                        <StackPanel Margin="12,6" VerticalAlignment="Center"><TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="11" /><TextBlock Text="{Binding ItemNumber}" Foreground="{StaticResource MutedBrush}" FontSize="9" /></StackPanel>
                                        <Border Grid.Column="1" Background="#ECE3D3" CornerRadius="10" Padding="8,3" HorizontalAlignment="Left" VerticalAlignment="Center"><TextBlock Text="{Binding Direction}" FontSize="9" FontWeight="Bold" /></Border>
                                        <TextBlock Grid.Column="2" Text="{Binding Quantity}" VerticalAlignment="Center" FontSize="11" />
                                        <TextBlock Grid.Column="3" Text="{Binding UnitPrice}" VerticalAlignment="Center" FontSize="11" />
                                        <TextBlock Grid.Column="4" Text="{Binding HourlyValue}" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="11" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <Border Grid.Row="1" Grid.ColumnSpan="2" Background="#A0FBF8EF">
            <Border.Style><Style TargetType="Border"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsLoading}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Border.Style>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"><ProgressBar Width="180" Height="5" IsIndeterminate="True" Foreground="{StaticResource GoldBrush}" /><TextBlock Text="Pricing every item flow…" Foreground="{StaticResource MutedBrush}" Margin="0,12,0,0" /></StackPanel>
        </Border>
    </Grid>
</UserControl>
